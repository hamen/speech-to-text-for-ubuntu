# Optional STT backend: NVIDIA Parakeet-TDT-0.6B-v3 (multilingual) on GPU.
# Install in a SEPARATE venv (keeps it apart from faster-whisper's CUDA libs):
#   uv venv -p 3.12 ~/parakeet-test/venv
#   ~/parakeet-test/venv/bin/pip install -r requirements-parakeet.txt
# Then point the menu at it (default) or:  export PARAKEET_PYTHON=~/parakeet-test/venv/bin/python
onnx-asr
onnxruntime-gpu<1.23  # CUDA 12.x — 1.27 needs CUDA 13 (libcudart.so.13), system has 12.4
huggingface_hub
