# Python dependencies for Speech-to-Text Key Listener
# Core dependencies
numpy
soundfile
pyautogui
evdev

# Note: the Nemotron STT server has its own venv (see requirements-nemotron.txt).
# This file lists only what the client (key_listener.py / speech_to_text.py) needs.